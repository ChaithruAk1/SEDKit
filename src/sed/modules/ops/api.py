"""Ops API routes mounted at /api/ops. Routes are declared in Phase 0 (P0.7); bodies belong to ws5-api-ops."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter()
