"""AI run lifecycle through Claude Code: start-run -> packets -> agent JSON -> ingest -> finish-run -> review.

Skills are module-owned (SkillDef + SkillHandler); this package is the generic lifecycle. See ai/CLAUDE.md.
"""
