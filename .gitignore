# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/

# Web
web/node_modules/
web/dist/

# Data never lives in the repo (DATA_DIR is <data root>/<profile>; see docs/data-location.md)
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
data/
out/
*.log

# Branding files (company logos) stay on this machine, in the branding folder under the data root: the images
# SED serves and the originals they came from (see `sed branding` and docs/data-location.md). This entry is a
# safety net for a logo dropped in the checkout by mistake; nothing is kept here.
/Logo/

# Secrets / machine-local config
.env
.env.*
.claude/settings.local.json

# Worktrees for parallel builds (docs/playbooks/parallel-build.md)
.claude/worktrees/
